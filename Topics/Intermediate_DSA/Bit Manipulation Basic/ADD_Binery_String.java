class ADD_Binery_String {
    public static void main(String[] args) {
        addBinary("1101", "11");

    }

    public static String addBinary(String A, String B) {
        int n = A.length();
        int m = B.length();

        String result = "";
        // int a = Integer.parseInt(A);
        // int b = Integer.parseInt(B);
        int a = 5;
        System.out.println(a = a ^ 1);

        int i = A.length() - 1;
        int j = B.length() - 1;
        int carry = 0;

        // while (i >= 0 || j >= 0 || carry > 0) {
        // int sum = 0;
        // }

        return result;
    }
}